import { createContext } from "react";

/*  Al usar createContext este nos facilita información que puede ser 
    compartida con los demás componentes del árbol de componentes */
export const AuthContext = createContext();